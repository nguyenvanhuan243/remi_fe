"use strict";

var _ = require("../");

var exportedUtils = _interopRequireWildcard(_);

var _fs = require("fs");

function _interopRequireWildcard(obj) { if (obj && obj.__esModule) { return obj; } else { var newObj = {}; if (obj != null) { for (var key in obj) { if (Object.prototype.hasOwnProperty.call(obj, key)) newObj[key] = obj[key]; } } newObj.default = obj; return newObj; } }

describe("documentation", function () {
  var documentationFileString = (0, _fs.readFileSync)("./README.md").toString();

  it("should have mentioned all exported utils", function () {
    var utilNames = Object.keys(exportedUtils);

    utilNames.forEach(function (name) {
      return expect(documentationFileString).toContain(name);
    });
  });
});

describe("types", function () {
  var typeScriptFileString = (0, _fs.readFileSync)("./src/types.d.ts").toString();

  it("should expose typescript definitions for each exported util", function () {
    var utilNames = Object.keys(exportedUtils);

    utilNames.forEach(function (name) {
      return expect(typeScriptFileString).toContain(name);
    });
  });
});